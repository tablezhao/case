# 调整高频时段定义

根据您的要求，我将把“本年通报高频时段”的筛选条件从 ≥ 10 次调整为 **> 5 次**。

## 执行计划

1.  **修改后端统计逻辑 (RPC)**
    *   创建新的 Supabase 迁移文件 `20251230150000_adjust_high_frequency_threshold.sql`。
    *   在 `get_trend_overview` 函数中，将 `high_frequency_months` 部分的 `HAVING` 子句修改为：
        ```sql
        HAVING COUNT(DISTINCT department_id || '_' || report_date) > 5
        ```
    *   执行 `supabase db push` 应用更改。

2.  **更新前端界面说明**
    *   修改 `src/components/trend/TrendOverviewSection.tsx` 文件。
    *   更新“本年通报高频时段”的 Tooltip 说明，明确筛选条件为“> 5次”。

3.  **验证更改**
    *   运行测试确保 API 依然正常工作。
    *   前端界面应显示更多符合条件的月份（如果有）。