## 问题分析

当前日期是2026-01-02，系统在显示环比数据时出现以下问题：
- 当选择"较上月"时，显示-100%，但2026年1月没有上月数据
- 当选择"较上季度"时，显示-100%，但2026年Q1没有上季度数据
- 当选择"较上年度"时，显示-100%，但2026年没有上年度数据

## 修复方案

在StatsCard组件中添加时间周期判断逻辑，根据当前日期和选择的时间维度，决定是否显示环比数据：

### 实现思路

1. **添加时间判断逻辑**：在StatsCard组件中添加函数，判断当前日期是否有足够的历史数据进行比较
2. **修改Props类型**：添加可选的"currentPeriod"参数，用于指定当前统计周期
3. **优化显示逻辑**：
   - 当没有足够历史数据时，显示"暂无数据"
   - 当有数据时，正常显示环比变化
   - 保持与现有UI风格一致

### 关键修改点

1. **StatsCard组件**：
   - 添加时间判断函数
   - 修改trend显示逻辑
   - 支持判断不同时间维度

2. **HomePage组件**：
   - 为StatsCard传递当前时间周期信息
   - 确保在2026年各时间维度下都能正确显示

### 预期效果

- 2026年1月选择"较上月"时，显示"暂无数据"
- 2026年1月选择"较上季度"时，显示"暂无数据"
- 2026年1月选择"较上年度"时，显示"暂无数据"
- 当2026年有足够数据时，正常显示环比变化

### 技术实现

- 使用JavaScript Date对象进行时间比较
- 基于当前日期和选择的时间维度判断
- 保持组件的复用性和可维护性

## 修复步骤

1. 修改StatsCard组件，添加时间判断逻辑
2. 更新StatsCardProps类型定义
3. 修改HomePage组件，传递必要的时间参数
4. 测试不同时间维度下的显示效果
5. 确保与现有代码风格一致

## 风险评估

- **低风险**：只修改前端显示逻辑，不影响后端数据
- **兼容性好**：保持现有API不变，向后兼容
- **可维护性强**：逻辑清晰，易于扩展和修改