# 调整风险等级分类标准

根据您的要求，我将把风险等级的分类标准调整为：
- **高风险**：通报频次 ≥ 10 次
- **中风险**：5 次 < 通报频次 < 10 次
- **低风险**：通报频次 ≤ 5 次

## 执行计划

1.  **修改后端统计逻辑 (RPC)**
    *   创建一个新的 Supabase 迁移文件 `20251230140000_adjust_risk_levels.sql`。
    *   在 `get_trend_overview` 函数中更新风险等级的判定逻辑：
        ```sql
        CASE
          WHEN COUNT(...) >= 10 THEN 'high'
          WHEN COUNT(...) > 5 THEN 'medium'
          ELSE 'low'
        END
        ```
    *   执行 `supabase db push` 应用更改。

2.  **更新前端界面说明**
    *   修改 `src/components/trend/TrendOverviewSection.tsx` 文件。
    *   更新 Tooltip 中的文字说明，使其与新的后端逻辑保持一致。

3.  **验证更改**
    *   确认前端显示的风险等级和 Tooltip 说明已更新。
    *   运行现有的一致性测试 `npm test src/db/consistency.test.ts` 确保数据获取依然正常。