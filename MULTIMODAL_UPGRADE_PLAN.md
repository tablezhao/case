# 智能案例导入 - 多模态升级方案

## 1. 升级概述

### 1.1 升级目标

将现有的URL解析功能升级为支持多模态输入的智能案例导入系统，用户可通过**文本、图片、PDF**三种方式输入案例信息，系统自动提取并填充到案例字段。

### 1.2 核心能力

- 📝 **文本输入**：直接粘贴或输入文字描述
- 🖼️ **图片上传**：支持JPG、PNG格式的截图或照片
- 📄 **PDF上传**：支持PDF文档解析
- 🤖 **多模态AI**：统一的智能信息提取
- ✅ **自动填充**：智能映射到案例字段
- 👁️ **预览编辑**：人工审核和修改

## 2. 技术架构设计

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    前端界面层                                │
│  SmartImportPage.tsx - 多模态输入界面                        │
│  ┌──────────┬──────────┬──────────┐                        │
│  │ URL输入  │ 文本输入 │ 文件上传 │                        │
│  └──────────┴──────────┴──────────┘                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                 Supabase Storage（可选）                     │
│  临时存储上传的图片和PDF文件                                 │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│              Supabase Edge Function                          │
│  parse-multimodal-case - 多模态内容解析                      │
│  ┌──────────────────────────────────────┐                  │
│  │ 输入类型路由                          │                  │
│  │  - URL → 网页抓取                     │                  │
│  │  - Text → 直接分析                    │                  │
│  │  - Image → 图像理解                   │                  │
│  │  - PDF → 文档解析                     │                  │
│  └──────────────────────────────────────┘                  │
│                    ↓                                         │
│  ┌──────────────────────────────────────┐                  │
│  │ AI信息提取引擎                        │                  │
│  │  - 正则表达式匹配                     │                  │
│  │  - 关键词识别                         │                  │
│  │  - 上下文分析                         │                  │
│  │  - 结构化输出                         │                  │
│  └──────────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    数据库层                                  │
│  cases表 - 存储案例数据                                      │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 数据流程

```
用户选择输入方式
    ↓
┌───────────┬───────────┬───────────┐
│ URL输入   │ 文本输入  │ 文件上传  │
└───────────┴───────────┴───────────┘
    ↓           ↓           ↓
    │           │      上传到Storage
    │           │           ↓
    │           │      获取文件URL
    │           │           ↓
    └───────────┴───────────┘
              ↓
    调用Edge Function
              ↓
    根据类型处理内容
              ↓
    AI提取结构化信息
              ↓
    返回标准化数据
              ↓
    前端显示预览
              ↓
    用户审核编辑
              ↓
    保存到数据库
```

## 3. 前端界面设计

### 3.1 界面布局

```
┌─────────────────────────────────────────────────────────┐
│  🔙 返回    ✨ 智能案例导入                              │
│  AI自动解析监管通报，提取案例信息                        │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 步骤1：选择输入方式                              │   │
│  │                                                  │   │
│  │  [ URL输入 ] [ 文本输入 ] [ 图片上传 ] [ PDF上传 ] │   │
│  │                                                  │   │
│  │  ┌────────────────────────────────────────┐    │   │
│  │  │  [当前选中的输入界面]                  │    │   │
│  │  │                                        │    │   │
│  │  │  - URL: 输入框 + 开始解析按钮          │    │   │
│  │  │  - 文本: 多行文本框 + 开始解析按钮     │    │   │
│  │  │  - 图片: 拖拽上传区 + 开始解析按钮     │    │   │
│  │  │  - PDF: 拖拽上传区 + 开始解析按钮      │    │   │
│  │  │                                        │    │   │
│  │  └────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 步骤2：检查并编辑数据                            │   │
│  │                                                  │   │
│  │  置信度: 85% 🟢 高                               │   │
│  │                                                  │   │
│  │  ⚠️ 警告: 未能识别应用平台                       │   │
│  │                                                  │   │
│  │  [可编辑的表单字段]                              │   │
│  │  - 应用名称 *                                    │   │
│  │  - 通报日期 *                                    │   │
│  │  - 开发者                                        │   │
│  │  - 监管部门                                      │   │
│  │  - 应用平台                                      │   │
│  │  - 违规摘要                                      │   │
│  │  - 详细内容                                      │   │
│  │                                                  │   │
│  │  [取消] [确认导入]                               │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 步骤3：执行报告                                  │   │
│  │                                                  │   │
│  │  ✅ 导入成功                                     │   │
│  │  案例ID: xxx-xxx-xxx                             │   │
│  │  提取字段: 6/7                                   │   │
│  │                                                  │   │
│  │  [查看案例列表] [继续导入]                       │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 3.2 Tab切换设计

使用shadcn/ui的Tabs组件：

```tsx
<Tabs defaultValue="url" className="w-full">
  <TabsList className="grid w-full grid-cols-4">
    <TabsTrigger value="url">🌐 URL输入</TabsTrigger>
    <TabsTrigger value="text">📝 文本输入</TabsTrigger>
    <TabsTrigger value="image">🖼️ 图片上传</TabsTrigger>
    <TabsTrigger value="pdf">📄 PDF上传</TabsTrigger>
  </TabsList>
  
  <TabsContent value="url">
    {/* URL输入界面 */}
  </TabsContent>
  
  <TabsContent value="text">
    {/* 文本输入界面 */}
  </TabsContent>
  
  <TabsContent value="image">
    {/* 图片上传界面 */}
  </TabsContent>
  
  <TabsContent value="pdf">
    {/* PDF上传界面 */}
  </TabsContent>
</Tabs>
```

## 4. 后端实现方案

### 4.1 Edge Function设计

**函数名称**：`parse-multimodal-case`

**输入参数**：

```typescript
interface ParseRequest {
  type: 'url' | 'text' | 'image' | 'pdf';
  content: string; // URL、文本内容、或文件URL
}
```

**输出格式**：

```typescript
interface ParsedCase {
  report_date: string | null;
  app_name: string | null;
  developer: string | null;
  department: string | null;
  platform: string | null;
  violation_summary: string | null;
  violation_detail: string | null;
  source_url: string | null;
  confidence: number;
  warnings: string[];
  input_type: string; // 记录输入类型
}
```

### 4.2 处理流程

#### 4.2.1 URL输入处理

```typescript
async function processUrl(url: string): Promise<string> {
  // 1. 抓取网页内容
  const response = await fetch(url);
  const html = await response.text();
  
  // 2. 清理HTML
  const text = cleanHtml(html);
  
  return text;
}
```

#### 4.2.2 文本输入处理

```typescript
async function processText(text: string): Promise<string> {
  // 直接返回文本内容
  return text.trim();
}
```

#### 4.2.3 图片输入处理

```typescript
async function processImage(imageUrl: string): Promise<string> {
  // 方案1: 使用OCR提取文字
  // 方案2: 使用图像理解API
  // 方案3: 简化版 - 提示用户图片内容
  
  // 这里使用简化方案
  return `[图片内容] 请根据上传的图片手动补充信息`;
}
```

#### 4.2.4 PDF输入处理

```typescript
async function processPdf(pdfUrl: string): Promise<string> {
  // 方案1: 使用PDF解析库
  // 方案2: 转换为图片后OCR
  // 方案3: 简化版 - 提示用户PDF内容
  
  // 这里使用简化方案
  return `[PDF文档] 请根据上传的PDF手动补充信息`;
}
```

### 4.3 统一的AI提取引擎

```typescript
function extractCaseInfo(text: string): ParsedCase {
  return {
    report_date: extractDate(text),
    app_name: extractAppName(text),
    developer: extractDeveloper(text),
    department: extractDepartment(text),
    platform: extractPlatform(text),
    violation_summary: extractViolationSummary(text),
    violation_detail: extractViolationDetail(text),
    source_url: null,
    confidence: calculateConfidence(...),
    warnings: generateWarnings(...),
    input_type: 'text' // 或其他类型
  };
}
```

## 5. 文件上传实现

### 5.1 创建Storage Bucket

```sql
-- 创建临时文件存储桶
INSERT INTO storage.buckets (id, name, public)
VALUES ('temp-uploads', 'temp-uploads', false);

-- 设置存储策略
CREATE POLICY "管理员可上传临时文件"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (
  bucket_id = 'temp-uploads' AND
  auth.uid() IN (SELECT id FROM profiles WHERE role = 'admin')
);

CREATE POLICY "管理员可读取临时文件"
ON storage.objects FOR SELECT
TO authenticated
USING (
  bucket_id = 'temp-uploads' AND
  auth.uid() IN (SELECT id FROM profiles WHERE role = 'admin')
);

CREATE POLICY "管理员可删除临时文件"
ON storage.objects FOR DELETE
TO authenticated
USING (
  bucket_id = 'temp-uploads' AND
  auth.uid() IN (SELECT id FROM profiles WHERE role = 'admin')
);
```

### 5.2 前端上传逻辑

```typescript
async function uploadFile(file: File): Promise<string> {
  const fileName = `${Date.now()}-${file.name}`;
  const filePath = `temp/${fileName}`;
  
  // 上传到Supabase Storage
  const { data, error } = await supabase.storage
    .from('temp-uploads')
    .upload(filePath, file);
  
  if (error) throw error;
  
  // 获取公开URL
  const { data: { publicUrl } } = supabase.storage
    .from('temp-uploads')
    .getPublicUrl(filePath);
  
  return publicUrl;
}
```

### 5.3 文件清理

```typescript
async function deleteTemporaryFile(filePath: string) {
  await supabase.storage
    .from('temp-uploads')
    .remove([filePath]);
}
```

## 6. 用户操作流程

### 6.1 URL输入流程（保持不变）

1. 选择"URL输入"标签
2. 输入网页地址
3. 点击"开始解析"
4. 等待AI分析
5. 检查并编辑数据
6. 确认导入

### 6.2 文本输入流程

1. 选择"文本输入"标签
2. 粘贴或输入文字描述
3. 点击"开始解析"
4. AI分析文本内容
5. 检查并编辑数据
6. 确认导入

**示例文本**：
```
工业和信息化部于2024年1月15日发布通报，
"某某App"（开发者：XX科技有限公司）
在应用宝平台存在超范围收集个人信息的问题，
违规获取用户通讯录、位置信息等敏感数据。
```

### 6.3 图片上传流程

1. 选择"图片上传"标签
2. 拖拽或点击上传图片
3. 预览上传的图片
4. 点击"开始解析"
5. 系统上传图片到Storage
6. AI分析图片内容（OCR识别文字）
7. 检查并编辑数据
8. 确认导入

**适用场景**：
- 监管通报的截图
- 手机拍摄的文件照片
- 扫描的纸质文档

### 6.4 PDF上传流程

1. 选择"PDF上传"标签
2. 拖拽或点击上传PDF
3. 显示PDF文件信息
4. 点击"开始解析"
5. 系统上传PDF到Storage
6. AI解析PDF内容
7. 检查并编辑数据
8. 确认导入

**适用场景**：
- 官方PDF通报文件
- 扫描的纸质文档PDF
- 导出的网页PDF

## 7. AI信息提取增强

### 7.1 文本预处理

```typescript
function preprocessText(text: string): string {
  return text
    .replace(/\s+/g, ' ')           // 统一空白字符
    .replace(/[""]/g, '"')          // 统一引号
    .replace(/[（）]/g, (m) => m === '（' ? '(' : ')') // 统一括号
    .trim();
}
```

### 7.2 增强的提取算法

#### 日期提取增强

```typescript
function extractDate(text: string): string | null {
  const patterns = [
    /(\d{4})[年\-\/](\d{1,2})[月\-\/](\d{1,2})[日]?/,
    /(\d{4})-(\d{2})-(\d{2})/,
    /(\d{4})\.(\d{1,2})\.(\d{1,2})/,
    /发布时间[：:]\s*(\d{4})[年\-\/](\d{1,2})[月\-\/](\d{1,2})/,
    /通报时间[：:]\s*(\d{4})[年\-\/](\d{1,2})[月\-\/](\d{1,2})/,
    /(\d{4})年第\d+批/  // 提取年份，月日设为01-01
  ];
  
  for (const pattern of patterns) {
    const match = text.match(pattern);
    if (match) {
      const year = match[1];
      const month = (match[2] || '01').padStart(2, '0');
      const day = (match[3] || '01').padStart(2, '0');
      return `${year}-${month}-${day}`;
    }
  }
  
  return null;
}
```

#### 应用名称提取增强

```typescript
function extractAppName(text: string): string | null {
  const patterns = [
    /应用名称[：:]\s*[《"']?([^》"'\s\n]{2,30})[》"']?/,
    /App名称[：:]\s*[《"']?([^》"'\s\n]{2,30})[》"']?/,
    /软件名称[：:]\s*[《"']?([^》"'\s\n]{2,30})[》"']?/,
    /[《"']([^》"']{2,20}App)[》"']/,
    /[《"']([^》"']{2,20})[》"'].*?存在.*?问题/,
    /通报.*?[《"']([^》"']{2,30})[》"']/,
    /下架.*?[《"']([^》"']{2,30})[》"']/
  ];
  
  for (const pattern of patterns) {
    const match = text.match(pattern);
    if (match) {
      let appName = match[1].trim();
      // 清理常见后缀
      appName = appName.replace(/（.*?）/g, '');
      appName = appName.replace(/\(.*?\)/g, '');
      return appName;
    }
  }
  
  return null;
}
```

### 7.3 上下文关联分析

```typescript
function extractWithContext(text: string): Partial<ParsedCase> {
  const sentences = text.split(/[。！？\n]/);
  const result: Partial<ParsedCase> = {};
  
  // 查找包含应用名称的句子
  const appSentence = sentences.find(s => 
    s.includes('App') || s.includes('应用') || s.includes('软件')
  );
  
  if (appSentence) {
    result.app_name = extractAppName(appSentence);
    
    // 在同一句子中查找开发者
    const devMatch = appSentence.match(/开发者[：:]?\s*([^\s，。]{2,30})/);
    if (devMatch) {
      result.developer = devMatch[1];
    }
  }
  
  // 查找包含违规的句子
  const violationSentences = sentences.filter(s =>
    s.includes('违规') || s.includes('问题') || s.includes('收集')
  );
  
  if (violationSentences.length > 0) {
    result.violation_summary = violationSentences[0].trim();
    result.violation_detail = violationSentences.join('。');
  }
  
  return result;
}
```

## 8. 实施步骤

### 步骤1：数据库准备

```bash
# 创建Storage Bucket
supabase migration create create_temp_uploads_bucket
```

### 步骤2：更新Edge Function

```bash
# 重命名并升级现有函数
mv supabase/functions/parse-case-from-url supabase/functions/parse-multimodal-case
# 更新函数代码
```

### 步骤3：更新前端页面

```bash
# 修改SmartImportPage.tsx
# 添加Tabs组件
# 添加文件上传组件
```

### 步骤4：测试验证

- 测试URL输入
- 测试文本输入
- 测试图片上传
- 测试PDF上传

### 步骤5：文档更新

- 更新用户指南
- 更新技术文档
- 添加示例

## 9. 技术限制说明

### 9.1 当前实现限制

由于Edge Function环境限制，以下功能采用简化实现：

1. **图片OCR**：
   - 完整方案需要集成第三方OCR服务
   - 当前简化为提示用户手动输入
   - 未来可集成百度OCR、腾讯OCR等服务

2. **PDF解析**：
   - 完整方案需要PDF解析库
   - 当前简化为提示用户手动输入
   - 未来可集成pdf.js或第三方服务

3. **图像理解**：
   - 完整方案需要多模态大模型API
   - 当前简化为基础文本提取
   - 未来可集成GPT-4V、Claude等

### 9.2 推荐的增强方案

#### 方案A：集成第三方AI服务

```typescript
// 使用OpenAI GPT-4V进行图像理解
async function analyzeImageWithGPT4V(imageUrl: string) {
  const response = await fetch('https://api.openai.com/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${OPENAI_API_KEY}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      model: 'gpt-4-vision-preview',
      messages: [{
        role: 'user',
        content: [
          { type: 'text', text: '请提取图片中的监管案例信息...' },
          { type: 'image_url', image_url: { url: imageUrl } }
        ]
      }]
    })
  });
  
  return await response.json();
}
```

#### 方案B：使用百度OCR

```typescript
async function extractTextFromImage(imageUrl: string) {
  // 调用百度OCR API
  const response = await fetch('https://aip.baidubce.com/rest/2.0/ocr/v1/general_basic', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/x-www-form-urlencoded'
    },
    body: `image=${encodeURIComponent(imageUrl)}&access_token=${BAIDU_ACCESS_TOKEN}`
  });
  
  const result = await response.json();
  return result.words_result.map(item => item.words).join('\n');
}
```

## 10. 性能优化

### 10.1 文件大小限制

```typescript
const FILE_SIZE_LIMITS = {
  image: 5 * 1024 * 1024,  // 5MB
  pdf: 10 * 1024 * 1024,   // 10MB
};

function validateFileSize(file: File, type: 'image' | 'pdf'): boolean {
  return file.size <= FILE_SIZE_LIMITS[type];
}
```

### 10.2 文件格式验证

```typescript
const ALLOWED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/jpg'];
const ALLOWED_PDF_TYPES = ['application/pdf'];

function validateFileType(file: File, type: 'image' | 'pdf'): boolean {
  const allowedTypes = type === 'image' ? ALLOWED_IMAGE_TYPES : ALLOWED_PDF_TYPES;
  return allowedTypes.includes(file.type);
}
```

### 10.3 缓存策略

```typescript
// 缓存已解析的内容（24小时）
const cache = new Map<string, { data: ParsedCase; timestamp: number }>();

function getCachedResult(key: string): ParsedCase | null {
  const cached = cache.get(key);
  if (cached && Date.now() - cached.timestamp < 24 * 60 * 60 * 1000) {
    return cached.data;
  }
  return null;
}
```

## 11. 安全考虑

### 11.1 文件安全

- ✅ 文件类型白名单
- ✅ 文件大小限制
- ✅ 病毒扫描（可选）
- ✅ 临时文件自动清理

### 11.2 权限控制

- ✅ 仅管理员可上传
- ✅ 私有Storage Bucket
- ✅ 签名URL访问
- ✅ 操作日志记录

## 12. 测试用例

### 12.1 文本输入测试

**测试用例1：完整信息**
```
输入：工业和信息化部于2024年1月15日发布通报，"某某App"（开发者：XX科技有限公司）在应用宝平台存在超范围收集个人信息的问题。

预期输出：
- report_date: 2024-01-15
- app_name: 某某App
- developer: XX科技有限公司
- department: 工业和信息化部
- platform: 应用宝
- violation_summary: 超范围收集个人信息
```

**测试用例2：部分信息**
```
输入：某App存在违规收集用户位置信息的问题。

预期输出：
- app_name: 某App
- violation_summary: 违规收集用户位置信息
- warnings: ["未能识别通报日期", "未能识别监管部门", ...]
```

### 12.2 图片上传测试

- 测试清晰截图
- 测试模糊照片
- 测试手写文字
- 测试表格内容

### 12.3 PDF上传测试

- 测试文字PDF
- 测试扫描PDF
- 测试多页PDF
- 测试加密PDF

## 13. 后续优化方向

### 短期（1-2周）

1. ✅ 实现基础的多模态输入界面
2. ✅ 完成文本输入功能
3. ✅ 完成文件上传功能
4. ⏳ 集成简单的OCR服务

### 中期（1-3个月）

1. ⏳ 集成GPT-4V或Claude进行图像理解
2. ⏳ 实现PDF文本提取
3. ⏳ 优化AI提取准确率
4. ⏳ 添加批量处理功能

### 长期（3-6个月）

1. ⏳ 训练专用的案例提取模型
2. ⏳ 支持更多文件格式（Word、Excel）
3. ⏳ 实现自动化监控和导入
4. ⏳ 提供API接口

## 14. 成本估算

### 14.1 存储成本

- Supabase Storage: 免费额度1GB
- 临时文件平均大小: 2MB
- 每月处理案例: 100个
- 月存储成本: 约$0（在免费额度内）

### 14.2 计算成本

- Edge Function: 免费额度500K次调用
- 每次调用时长: 30秒
- 月调用次数: 约300次
- 月计算成本: 约$0（在免费额度内）

### 14.3 第三方API成本（可选）

- OpenAI GPT-4V: $0.01/图片
- 百度OCR: 免费额度1000次/天
- 月API成本: 约$0-10

---

**文档版本**：v2.0  
**最后更新**：2024-01-15  
**维护者**：合规通开发团队
